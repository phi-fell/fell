package com.monolc.fell.graphics;

public class Text {
	// text rendering for reference
	/*
	 * public void displayText(BufferedImage image, String text, Font font, int
	 * x, int y){ Graphics2d g = image.createGraphics(); g.setFont(font);
	 * g.drawString(text, x, y); g.dispose(); }
	 */
}
